using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// A script that implements this can be activated by ContextActionController when the player presses the interact button
public interface IContextInteractable
{
    void ContextInteraction();
}
